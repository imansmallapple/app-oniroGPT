import file from '@ohos.file.fs'; //用于读取普通文件
import image from '@ohos.multimedia.image';


export class ImageUtils {
  private imageContent: string = '';

  constructor(imageContent: string) {
    this.imageContent = imageContent
  }

  openImage() {
    // getImage
  }
}
