export default class Constants{
  public static readonly CREATE_MESSAGE_LIST_TABLE_SQL = 'CREATE TABLE IF NOT EXISTS MESSAGEITEM (ID TEXT PRIMARY KEY, NAME  TEXT NOT NULL, QUANTITY INTEGER DEFAULT 1,CATEGORY TEXT NOT NULL, DONE INTEGER DEFAULT 0)'; // 建表Sql语句

}